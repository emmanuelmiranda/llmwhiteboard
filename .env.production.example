# LLM Whiteboard - Production Environment Configuration
# Copy this file to .env and configure for your deployment

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# JWT Secret Key (generate with: openssl rand -base64 32)
# IMPORTANT: Change this to a secure random value!
JWT_KEY=your-secret-key-at-least-32-characters-long-change-in-production

# =============================================================================
# DATABASE SETTINGS
# =============================================================================

POSTGRES_USER=llmwhiteboard
POSTGRES_PASSWORD=llmwhiteboard
POSTGRES_DB=llmwhiteboard

# =============================================================================
# PORT CONFIGURATION
# =============================================================================

# Database port (internal PostgreSQL)
DB_PORT=22432

# Backend API port
API_PORT=22001

# Frontend web port
WEB_PORT=22000

# =============================================================================
# URL CONFIGURATION
# =============================================================================

# URL where the frontend will be accessible (for CORS)
FRONTEND_URL=https://llmwhiteboard.com

# URL where the backend API is accessible (from browser)
API_URL=https://api.llmwhiteboard.com

# For the frontend to call the API (used at build time for Next.js)
NEXT_PUBLIC_API_URL=https://api.llmwhiteboard.com

# =============================================================================
# JWT SETTINGS
# =============================================================================

# Number of days before JWT tokens expire
JWT_EXPIRY_DAYS=7

# =============================================================================
# AUTHENTICATION PROVIDERS
# =============================================================================

# Email/password authentication (disabled by default)
# Set to true to allow users to sign up and log in with email/password
AUTH_EMAIL_ENABLED=false

# GitHub OAuth (optional - leave empty to disable)
# To enable "Sign in with GitHub":
# 1. Go to GitHub Settings > Developer settings > OAuth Apps > New OAuth App
# 2. Set Authorization callback URL to: https://yourdomain.com/auth/github/callback
# 3. Copy Client ID and Client Secret here
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# =============================================================================
# CONTAINER VERSION
# =============================================================================

# Version tag of container images to pull (e.g., latest, v1.0.0)
VERSION=latest

# =============================================================================
# PRODUCTION DEPLOYMENT NOTES
# =============================================================================
#
# For production deployments:
#
# 1. Change JWT_KEY to a secure random value
# 2. Change POSTGRES_PASSWORD to a secure password
# 3. Update FRONTEND_URL and API_URL to your actual domain/IP
#    Example for a server at 192.168.1.100:
#      FRONTEND_URL=http://192.168.1.100:22000
#      API_URL=http://192.168.1.100:22001
#      NEXT_PUBLIC_API_URL=http://192.168.1.100:22001
#
# 4. For HTTPS, use a reverse proxy (nginx, traefik, caddy) and update URLs:
#      FRONTEND_URL=https://llmwhiteboard.com
#      API_URL=https://api.llmwhiteboard.com
#      NEXT_PUBLIC_API_URL=https://api.llmwhiteboard.com
#
